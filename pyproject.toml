[project]
name = "local-brain-py"
version = "0.1.0"
description = "Skill-based LLM tasks using local Ollama models"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ollama>=0.6.1",
    "pyyaml>=6.0.0",
    "jinja2>=3.0.0",
]
